%h1 TODO LIST
%table
  %thead
    %tr
      %th TODO NAME
  %tbody
    - @tasks.each do |task|
      %tr
        %td= task.title
        %td= link_to '編集', edit_task_path(task)
        %td= button_to '削除', task_path(task), method: :delete
= link_to '追加', new_task_path
